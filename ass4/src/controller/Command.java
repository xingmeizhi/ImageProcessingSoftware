package controller;

/**
 * This class represents an enum that contains
 * all the possible command.
 */
public enum Command {
  newProject,
  loadProject,
  saveProject,
  addLayer,
  addImageToLayer,
  setFilter,
  saveImage,
  quit
}
